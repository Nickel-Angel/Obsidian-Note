linux：一切设备皆文件
`/bin` 就是一个存储二进制文件的目录，里面有许多常用的用户程序，例如 `ls` `grep` 等
`/superbin` 是一个存储二进制文件的目录，但是里面的二进制程序只有一定权限的用户才能使用。
`/usr` 是用户文件夹，每个用户文件夹中都有自己的东西x
`/dev` 是外设，里面有硬盘等等内容
`/etc` 是配置文件
`/media` `/mnt` 是 usb 和光盘挂载点，前者是自动挂载点，后者是手动挂载点
`/opt` 是可以一些第三方插件
