#### 前端界面
```text
|-- 牌组记忆主界面 (CardMemoMain)
    |-- 牌组记忆 (CardMemoDisplay)
    |-- 牌组学习数据分析
|-- 卡片编辑主界面 (CardEditMain)
    |-- 卡片编辑
    |-- 新增牌组
|-- 设置主界面 (SettingsMain)
```

需要一个选项卡
##### 主界面（牌组学习界面）
列出当前的牌组（列表）
列表的主要内容是 牌组名称 未学习的 学习中 待复习
点击每个项即可进入学习
可以点击每个卡牌末尾的设置符号进入对应的卡牌编辑页面

##### 牌组编辑次级界面——牌组编辑

牌组记忆页面，会按照牌组类型渲染题目，并且后端会根据 FSRS 算法提供调度
题目可以根据对错来判断学生对于卡牌的掌握情况

大概的结构：


##### 牌组学习数据分析

##### 卡片编辑界面
会列出所有卡组的题目，可以根据题目所属的卡组和题目的类型对题目进行筛选和管理

##### 牌组编辑次级界面——卡片编辑
右侧根据题目的不同展示相应的内容

##### 牌组编辑次级界面——新建牌组
额外弹出一个对话框，可以选择手动录入或自动导入

##### 设置
选择题打乱顺序功能
设置目标留存率
查看 FSRS 参数

##### 后端
后端需要一个数据库，用于记录牌组题库和模板的配置

模板大概有如下两种：
* 有明确正反面的题目，类似于单词记忆卡
* 单选（判断），多选
模板的形式是：
题面（正面），答案（背面），解析

数据库表：
牌组表（<u>牌组编号</u>，牌组名称，未学习，正在学习，复习）
题目表（<u>题目编号</u>，牌组编号，题目类型编号，正面内容，背面内容，解析）
题目信息表（<u>题目编号，时间戳</u>，做题情况）

我需要做如下查询工作：
1. 所有牌组的查询，每个牌组未学习，学习中，待复习的卡片数量的查询
2. 卡牌所在牌组，首字段，预计复习时间的查询，并且需要根据牌组，学习状态，牌组模板筛选卡牌
3. 近 7 天复习记录的查询
4. 近 14 天复习卡牌的数量分布情况

# CardMemo 文件夹文档
## 概述
CardMemo 文件夹包含了与卡片记忆功能相关的组件，主要负责展示牌组信息、学习统计和卡片学习界面。这些组件共同构成了应用程序的记忆学习功能模块。

## 主要组件
### CardMemoMain.tsx
主要的牌组记忆页面组件，展示所有可用的牌组及其学习状态。
功能 ：
- 以表格形式展示所有牌组
- 显示每个牌组的未学习、学习中和待复习卡片数量
- 点击牌组名称可打开学习准备界面
- 显示今日学习统计信息
### CardMemoStart.tsx
牌组学习准备页面，显示牌组详细信息和学习统计。
功能 ：
- 显示牌组名称
- 展示未学习、学习中和待复习卡片的数量
- 提供"开始学习"按钮，点击后进入学习界面
- 如果没有卡片需要学习，则禁用开始按钮
属性 ：

```typescript
interface CardMemoStartProps {
  deckName: string;      // 牌组名称
  tolearn: number;       // 未学习卡片数量
  learning: number;      // 学习中卡片数量
  toreview: number;      // 待复习卡片数量
  onStartStudy: () => void; // 开始学习的回调函数
}
 ```
### CardMemoLearning.tsx
卡片学习界面，用于展示卡片内容并处理用户的学习反馈。
功能 ：
- 显示卡片正面内容
- 点击后显示卡片背面内容
- 提供评分按钮（忘记、困难、良好、简单）
- 根据用户评分调整卡片的复习计划
### CardMemoText.tsx
用于展示卡片内容的组件，支持正反面切换和评分功能。
功能 ：
- 显示卡片正面内容
- 点击"显示背面"按钮后显示卡片背面内容
- 显示背面后提供评分按钮
- 将用户评分传递给父组件
属性 ：

```typescript
interface CardMemoTextProps {
  frontContent: string;             // 卡片正面内容
  backContent: string;              // 卡片背面内容
  handleRating?: (rating: number) => void; // 评分回调函数
}
 ```
## 数据结构
### DeckData
表示牌组数据的接口：
```typescript
interface DeckData {
  deckId: number;    // 牌组ID
  name: string;      // 牌组名称
  tolearn: number;   // 未学习卡片数量
  learning: number;  // 学习中卡片数量
  toreview: number;  // 待复习卡片数量
}
 ```
## 工作流程
1. 用户在 CardMemoMain 页面查看所有牌组
2. 点击牌组名称，弹出 CardMemoStart 模态框
3. 用户点击"开始学习"按钮
4. 系统加载 CardMemoLearning 组件，开始学习过程
5. 用户通过 CardMemoText 组件查看卡片内容并进行评分
6. 根据评分调整卡片的复习计划
## 待完成功能
- 连接后端 API 获取真实牌组数据
- 实现卡片评分后的间隔重复算法
- 添加学习进度保存功能
- 完善学习统计数据的展示
- 添加删除卡组的功能
- 自动检测 database 为空时重建模板时，应判断是否有相应模板

